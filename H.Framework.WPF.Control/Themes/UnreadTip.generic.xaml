<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:H.Framework.WPF.Infrastructure.Converters;assembly=H.Framework.WPF.Infrastructure"
    xmlns:local="clr-namespace:H.Framework.WPF.Control.Controls">
    <converter:ObjectConverter x:Key="objConverter" />
    <Style TargetType="{x:Type local:UnreadTip}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:UnreadTip}">
                    <Canvas x:Name="mPanel">
                        <Ellipse
                            Name="PART_Ellipse"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            Fill="{TemplateBinding Background}" />
                        <TextBlock
                            Name="PART_TextBlock"
                            Margin="0.6,0,0,0"
                            FontSize="8"
                            Foreground="White"
                            Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            TextAlignment="Center"
                            Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsShowText, Converter={StaticResource objConverter}, ConverterParameter=true:Visible:Collapsed}" />
                    </Canvas>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=PART_TextBlock, Path=Text.Length}" Value="1" />
                                <Condition Binding="{Binding ElementName=PART_TextBlock, Path=Visibility}" Value="Visible" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter TargetName="PART_Ellipse" Property="Width" Value="12" />
                                <Setter TargetName="PART_TextBlock" Property="Margin" Value="3.5,0,0,0" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=PART_TextBlock, Path=Text.Length}" Value="2" />
                                <Condition Binding="{Binding ElementName=PART_TextBlock, Path=Visibility}" Value="Visible" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter TargetName="PART_Ellipse" Property="Width" Value="12" />
                                <Setter TargetName="PART_TextBlock" Property="Margin" Value="1.2,0,0,0" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>